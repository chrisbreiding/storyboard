<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Storyboard</title>
  {{#each stylesheets}}
    <link rel="stylesheet" href="{{this}}" />
  {{/each}}
</head>
<body>
  <div class="loader"></div>
  <div id="app"></div>

  {{#each scripts}}
    <script src="{{this}}"></script>
  {{/each}}
</body>
</html>
